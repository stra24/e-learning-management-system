spring:
  datasource:
    # デバッグ用
    url: jdbc:postgresql://localhost:5433/elms_db
    username: root
    password: pass

    # 開発環境用
    # url: jdbc:postgresql://elms-db:5432/elms_db
    # username: root
    # password: pass

    # 本番環境用
    # url: jdbc:postgresql://${DB_HOST}:${DB_PORT}/${DB_NAME}
    # username: ${DB_USER}
    # password: ${DB_PASS}

  # 本番環境用
  # config:
  #   import: optional:file:.env[.properties] # 環境変数は.envの内容を参照する。
  flyway:
    enabled: true
    locations: classpath:db/migration