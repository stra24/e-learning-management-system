spring:
  datasource:
    # デバッグ用
    url: jdbc:postgresql://localhost:5433/elms_db
    username: root
    password: pass

    # 開発環境用
    # url: jdbc:postgresql://elms-db:5432/elms_db
    # username: root
    # password: pass

    # 本番環境用
    # url: jdbc:postgresql://${DB_HOST}:${DB_PORT}/${DB_NAME}
    # username: ${DB_USER}
    # password: ${DB_PASS}

  # 本番環境用
  # config:
  #   import: optional:file:.env[.properties] # 環境変数は.envの内容を参照する。
  flyway:
    enabled: true
    locations: classpath:db/migration

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-handlers-package: com.everrefine.elms.infrastructure.typehandler
  configuration:
    map-underscore-to-camel-case: true

jwt:
  # デバッグ環境 or 開発環境用
  secret: 0dTIXBDkSbhltNOJX8M3oMuN5K+vDG4Z7ZaY8rpg8kQ=
  # 本番環境用
  # secret: ${JWT_SECRET}

# HTTPS化するための設定
#server:
#  port: 8443
#  ssl:
#    enabled: true
#    key-store: classpath:keystore.p12
#    key-store-password: password
#    key-store-type: PKCS12

upload:
  directory: uploads # ファイルアップロード先ディレクトリ